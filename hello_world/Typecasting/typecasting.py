# Typecasting = the process of converting a variable from one type to another.
#               str(), int(), float(), and bool() are the built-in functions for typecasting.

name = "Eric"
age = 35
gpa = 3.75
is_student = True

gpa = int(gpa)  # Convert float to int
age = str(age)  # Convert int to str
is_student = str(is_student)  # Convert bool to str